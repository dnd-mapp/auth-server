info:
  name: soft-remove-by-id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{SERVER_URL}}/users/:user_id"
  params:
    - name: user_id
      value: WhBuArgL5YrhLtCq5PQXu
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
