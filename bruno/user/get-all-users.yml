info:
  name: get-all-users
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/users"
  params:
    - name: includeDeactivated
      value: "true"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
