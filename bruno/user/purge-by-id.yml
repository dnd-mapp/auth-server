info:
  name: purge-by-id
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{SERVER_URL}}/users/:user_id/purge"
  params:
    - name: user_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
