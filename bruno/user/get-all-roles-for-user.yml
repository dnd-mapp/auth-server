info:
  name: get-all-roles-for-user
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER_URL}}/users/:user_id/roles"
  params:
    - name: user_id
      value: sc8k1OyhsXBbv64w7Enl8
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
