info:
  name: liveness
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/health/liveness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
