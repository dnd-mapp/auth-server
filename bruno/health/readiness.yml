info:
  name: readiness
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_URL}}/health/readiness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
