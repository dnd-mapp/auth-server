info:
  name: update-by-id
  type: http
  seq: 3

http:
  method: PUT
  url: "{{SERVER_URL}}/roles/:role_id"
  params:
    - name: role_id
      value: ""
      type: path
  body:
    type: json
    data: |
      {
        "name": "Test role"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
