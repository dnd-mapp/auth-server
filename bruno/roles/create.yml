info:
  name: create
  type: http
  seq: 4

http:
  method: POST
  url: "{{SERVER_URL}}/roles"
  body:
    type: json
    data: |
      {
        "name": "Test role"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
